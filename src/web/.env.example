# 前端环境变量示例。复制为 .env 或 .env.local 后按需修改。
# 仅以 VITE_ 前缀开头的变量会被暴露到客户端代码中。

# 后端 API 基础地址（用于绝对路径请求；相对路径 /api 走 Vite 代理）
VITE_API_BASE_URL=http://localhost:3000

# 应用显示名称
VITE_APP_NAME=Vite React shadcn Template
